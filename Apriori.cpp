#include "Apriori.hpp"

namespace AssociationRules {

template <typename T, typename N>
void Apriori<T, N>::apriori_gen(const T & data, const double & conf, const double & acc) {
    /*
    
    */
}

}